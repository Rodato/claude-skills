# Conexión Postgres a Supabase — usar el POOLER (puerto 6543), no el host directo.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:6543/postgres

# Opcional: fecha de lanzamiento del bot (para métricas "desde el inicio").
BOT_START_DATE=2026-01-01

# Opcional: solo si generás narrativa de reportes con LLM (ver references/reportes.md).
# OPENROUTER_API_KEY=
